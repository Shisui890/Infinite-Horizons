{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm --prefix infinite run build",
  "installCommand": "npm --prefix infinite ci",
  "outputDirectory": "infinite/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
